import { SafeAreaView, StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { initializeApp } from "firebase/app";

  

const Home = () => {
  return (
    <SafeAreaView>
      <Text>Home Viewing</Text>
    </SafeAreaView>
  )
}


export default Home

const styles = StyleSheet.create({})
